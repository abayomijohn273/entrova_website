import Services from "@/modules/services"

export const metadata = {
  title: "Services | Entrova",
  description: "Full-cycle software development — web, mobile, cloud, and product design built to scale.",
}

const Page = () => {
  return (
    <Services />
  )
}

export default Page
